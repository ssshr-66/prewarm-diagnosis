package com.prewarm.diagnosis.contract.conversation;

/**
 * 会话上下文的数据契约。
 *
 * <p>后续在这里定义消息中提取出的意图、集群、消息 ID、路径和时间范围等字段；
 * 当前只保留架构占位，不实现序列化和字段校验。</p>
 */
public final class DiagnosisSessionContext {
    // TODO: 定义跨 Agent 层传递的结构化诊断上下文。
}
