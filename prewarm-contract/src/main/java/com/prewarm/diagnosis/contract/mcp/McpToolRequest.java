package com.prewarm.diagnosis.contract.mcp;

/**
 * Agent 调用诊断 MCP 工具时使用的内部请求契约。
 *
 * <p>真实 MCP SDK 接入后，再将该契约映射到具体协议对象。</p>
 */
public final class McpToolRequest {
    // TODO: 定义工具名、集群范围、时间范围和受控查询参数。
}
